import { i as inject } from "../services/current-app.mjs";
import { c as createGlobalConfig, p as provideForCurrentApp, G as GLOBAL_CONFIG } from "../services/global-config/global-config.mjs";
function useGlobalConfig() {
  let injected = inject(GLOBAL_CONFIG);
  if (!injected) {
    injected = createGlobalConfig();
    provideForCurrentApp(injected);
  }
  return injected;
}
export {
  useGlobalConfig as u
};
//# sourceMappingURL=useGlobalConfig.mjs.map

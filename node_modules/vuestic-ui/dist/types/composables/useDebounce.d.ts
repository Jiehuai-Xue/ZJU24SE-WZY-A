import { Ref } from 'vue';
export declare const useDebounce: <T>(onUpdate: () => any, timeout: Ref<number> | number) => {
    debounced: import("lodash").DebouncedFunc<() => void>;
};
/** Used for debounced callbacks. Can be used to show cb near debounce call in code structure. */
export declare const useDebounceFn: (timeout: Ref<number> | number) => {
    debounced: (cb: (() => void)) => void;
    cancel: () => void;
};

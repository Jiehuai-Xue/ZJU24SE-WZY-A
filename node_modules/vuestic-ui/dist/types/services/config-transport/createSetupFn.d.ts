import { DefineComponent, SetupContext } from 'vue';
import { type Props } from './shared';
export declare const createSetupFn: <T extends DefineComponent>(component: T) => (originalProps: Props, ctx: SetupContext) => void | {};

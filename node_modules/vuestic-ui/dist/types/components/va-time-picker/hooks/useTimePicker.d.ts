import { Ref } from 'vue';
import { TimePickerProps, ModelValueRef } from '../types';
export declare const useTimePicker: (props: TimePickerProps, modelValue: ModelValueRef) => {
    columns: import("vue").ComputedRef<({
        items: number[];
        activeItem: import("vue").WritableComputedRef<number>;
    } | {
        items: string[];
        activeItem: import("vue").WritableComputedRef<number>;
    })[]>;
    isPM: Ref<boolean>;
};

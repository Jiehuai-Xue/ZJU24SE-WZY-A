import { ModalOptions } from '../types';
/** This hook can be used without plugin used */
export declare const useModal: () => {
    init: (options: string | ModalOptions) => import("vue").VNode<import("vue").RendererNode, import("vue").RendererElement, {
        [key: string]: any;
    }>;
    confirm: (options: string | ModalOptions) => Promise<boolean>;
};
